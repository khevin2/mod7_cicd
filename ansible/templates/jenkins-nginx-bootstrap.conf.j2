server {
    listen 80;
    server_name {{ jenkins_server_name }};

    location ^~ /.well-known/acme-challenge/ {
        root {{ jenkins_acme_webroot }};
    }

    location / {
        return 404;
    }
}
